import { useEffect, useState } from 'react'
import { Box, Container, SimpleGrid, Heading, VStack, AspectRatio, Button, HStack, Text, useDisclosure, Modal, ModalContent, ModalOverlay, ModalBody } from "@chakra-ui/react"
import Image from 'next/image'
import { Section } from './Section'
// A “Music” section (not required to have working audio playback). Can display anything 
export const AlbumsItems = ({}) => {
    

    return(
        <Section>
        
        
        </Section>
    )
}